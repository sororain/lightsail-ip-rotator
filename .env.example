# AWS 凭证（必填）
AWS_ACCESS_KEY_ID=YOUR_ACCESS_KEY
AWS_SECRET_ACCESS_KEY=YOUR_SECRET_KEY

# AWS 区域（必填，多个用逗号分隔）
AWS_REGIONS=ap-northeast-1

# Ping 检测时长，单位秒（可选，默认 60）
PING_TIMEOUT=60

# 检测间隔，单位分钟（可选，默认 1）
CHECK_INTERVAL_MIN=1

# Server酱 推送 Token（可选，留空不推送）
SERVER_CHAN_TOKEN=
